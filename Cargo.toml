[workspace]
members = [
    "programs/dusk",
    "programs/leverage_delegate",
    "programs/faucet",
    "tests/fixtures/referral_transfer_hook",
    "tests/fixtures/token_metadata_fixture",
]
resolver = "2"

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1
opt-level = "z"

[profile.release.build-override]
opt-level = 3
incremental = false
codegen-units = 1
